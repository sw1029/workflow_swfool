interface:
  display_name: "Manage Agent Authority"
  short_description: "Manage bounded session and operation authority"
  default_prompt: "Use $manage-agent-authority to inspect or advance exact operation authority and reuse a bounded host-session approval."
