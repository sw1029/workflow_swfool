interface:
  display_name: "Manage Agent Authority"
  short_description: "Compile and settle exact authority safely"
  default_prompt: "Use $manage-agent-authority to compile exact authority operations, materialize approved recovery, and settle registered owner results."
