interface:
  display_name: "Manage Agent Authority"
  short_description: "Manage scoped authority grants and leases"
  default_prompt: "Use $manage-agent-authority to classify, evaluate, reserve, and audit an exact operation without expanding current permissions or rewriting historical authority."
