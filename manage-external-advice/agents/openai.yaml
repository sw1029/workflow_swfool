interface:
  display_name: "Manage External Advice"
  short_description: "Normalize non-GT advice inputs"
  default_prompt: "Use $manage-external-advice to intake, normalize, audit, inject, and retire external advice Markdown without treating it as goal truth, including stale metrics and re-advised dead root-cause checks."
