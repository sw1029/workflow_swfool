interface:
  display_name: "Optimize Task Slice"
  short_description: "Classify task slices including structure work"
  default_prompt: "Use $optimize-task-slice to classify whether the next task should be batched, narrowed, root-cause repaired, semantic consolidation, reuse extraction, coupling reduction, or blocked before derive."
