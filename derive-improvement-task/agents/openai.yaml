interface:
  display_name: "Derive Improvement Task"
  short_description: "Derive task.md with constrained gates"
  default_prompt: "Use $derive-improvement-task to verify the predecessor finalization receipt and authoritative projection before ordinary derivation, then select the next task.md or task-pack transaction from goal, authority, advice, loopback, structure, convention, and blocker evidence."
