interface:
  display_name: "Derive Improvement Task"
  short_description: "Derive a validated next task or terminal state"
  default_prompt: "Use $derive-improvement-task to freeze one exact evidence manifest, obtain exactly three independent candidate lenses, and synthesize one validated next task, task-pack disposition, or disjoint terminal outcome."
