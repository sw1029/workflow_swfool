interface:
  display_name: "Record Agent Work Log"
  short_description: "Write or safely migrate .agent_log records"
  default_prompt: "Use $record-agent-work-log to record a durable work log or migrate a legacy log store without changing historical bodies."
