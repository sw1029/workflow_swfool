interface:
  display_name: "Manage Task State Index"
  short_description: "Link task workflow artifacts by ID"
  default_prompt: "Use $manage-task-state-index with deterministic helpers first and configured Tier 2 model_ref:balanced/medium for delegated ID-only work, while linking task, task_pack, issue, goal, advice, schema/contract, execution, audit, and validation artifacts."
