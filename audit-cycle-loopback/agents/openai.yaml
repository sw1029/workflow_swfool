interface:
  display_name: "Audit Cycle Loopback"
  short_description: "Compute anti-loop and root-cause gates"
  default_prompt: "Use $audit-cycle-loopback to compute a conservative anti-loop progress gate, including root-cause untried/exhaustion fields, before deriving the next task."
