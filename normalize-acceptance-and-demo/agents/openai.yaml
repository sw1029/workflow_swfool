interface:
  display_name: "Normalize Acceptance And Demo"
  short_description: "Compile semantic acceptance into bound results"
  default_prompt: "Use $normalize-acceptance-and-demo to compile semantic acceptance criteria into a deterministic task-bound owner result."
