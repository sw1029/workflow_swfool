interface:
  display_name: "Validate Task Completion"
  short_description: "Validate completion and routing gates"
  default_prompt: "Use $validate-task-completion to evaluate completion and progress separately, emit the governed six-axis final candidate without durable publication, and require orchestrator finalization plus a verified receipt before any close or downstream consumption."
