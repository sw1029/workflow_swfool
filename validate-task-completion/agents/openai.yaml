interface:
  display_name: "Validate Task Completion"
  short_description: "Validate completion and routing gates"
  default_prompt: "Use $validate-task-completion to validate task completion, progress verdict, external advice handling, task-kind routing constraints, code convention conformance, structure high-water movement, audits, logs, issues, and task index evidence."
