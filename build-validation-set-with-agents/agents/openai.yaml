interface:
  display_name: "Build Validation Set"
  short_description: "Create reusable validation sets safely"
  default_prompt: "Use $build-validation-set-with-agents with deterministic oracles first, configured Tier 3 semantic labeling, and at most one configured Tier 4 adjudicator."
