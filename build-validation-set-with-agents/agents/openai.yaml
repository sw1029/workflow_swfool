interface:
  display_name: "Build Validation Set"
  short_description: "Create reusable validation sets safely"
  default_prompt: "Use $build-validation-set-with-agents to plan or build a source-traced validation set for the current task."
