interface:
  display_name: "Deep Interview Goal Context"
  short_description: "Interview for goal and authority GT"
  default_prompt: "Use $deep-interview-goal-context to continue .interview one question at a time, use $manage-agent-authority for agent-authority drafts/final writes, then write final goal files only to .agent_goal after confirmation passes."
