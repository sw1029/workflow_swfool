interface:
  display_name: "Validate Subskill Result Contract"
  short_description: "Validate cycle subskill result fields"
  default_prompt: "Use $validate-subskill-result-contract to check a subskill result before the cycle advances."
