interface:
  display_name: "Validate Subskill Result Contract"
  short_description: "Validate bounded workflow result contracts"
  default_prompt: "Use $validate-subskill-result-contract to validate hydrated owner results, exact bindings, and governed workflow boundaries before promotion."
