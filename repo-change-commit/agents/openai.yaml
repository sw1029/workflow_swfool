interface:
  display_name: "Repo Change Commit"
  short_description: "Create one verified goal-aware Git commit"
  default_prompt: "Use $repo-change-commit to stage a coherent change set and create one embedded, verifiable closeout commit."
