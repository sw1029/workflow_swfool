interface:
  display_name: "Maintain Cycle Ledger"
  short_description: "Maintain compact task-cycle ledgers"
  default_prompt: "Use $maintain-cycle-ledger to append compact content-bound cycle events, hydrate exact result artifacts, and finalize validated cycle state."
