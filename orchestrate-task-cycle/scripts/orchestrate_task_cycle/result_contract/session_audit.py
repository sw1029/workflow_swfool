"""Session-audit consumer public facade."""

# Compatibility facade intentionally re-exports imported symbols.
# ruff: noqa: F401

from ._session_audit.collection import (
    _index_metadata,
    collect_session_audit_directory,
    sanitize_collection_summary,
)
from ._session_audit.core import (
    ALLOWED_TOP_LEVEL_FIELDS,
    ARTIFACT_KIND,
    AUDIT_ID_RE,
    AUTO_REPAIR_TARGET,
    BINDING_STATUSES,
    CANONICAL_EVIDENCE_CLASSES,
    CANONICAL_EXACT_PATHS,
    CANONICAL_PATH_PREFIXES,
    CAPTURE_MODES,
    CAPTURE_STATUSES,
    EVIDENCE_CLASSES,
    FINDING_SEVERITIES,
    FORBIDDEN_BODY_KEYS,
    FORMAT_VERSION,
    HASH_URN_RE,
    INDEX_ARTIFACT_KIND,
    INTEGRITY_STATUSES,
    MAX_PACKET_BYTES,
    PARSER_VERSION,
    REPAIR_CLASSES,
    REQUIRED_TOP_LEVEL_FIELDS,
    SAFE_ID_RE,
    SHA256_RE,
    TOOLS,
    _add,
    _canonical_packet_bytes,
    _forbidden_body_paths,
    _is_governed_canonical_path,
    _is_non_empty_string,
    _is_safe_relative_path,
    _is_string_list,
    canonical_evidence_ref_hashes,
    canonical_evidence_refs,
    expected_audit_id,
    expected_index_id,
)
from ._session_audit.packet import (
    _bundled_projection_validator,
    sha256_file,
    validate_session_audit_packet,
    verify_canonical_evidence_refs,
    verify_deterministic_source_projection,
    verify_packet_source,
)
from ._session_audit.projection import (
    _invalid_projection,
    _safe_file_metadata,
    project_valid_packet,
    validate_session_audit_index,
)
from ._session_audit.projection_validation import (
    PROJECTION_PACKET_FIELDS,
    validate_collection_projection,
)


__all__ = [
    "ALLOWED_TOP_LEVEL_FIELDS",
    "ARTIFACT_KIND",
    "AUDIT_ID_RE",
    "AUTO_REPAIR_TARGET",
    "BINDING_STATUSES",
    "CANONICAL_EVIDENCE_CLASSES",
    "CANONICAL_EXACT_PATHS",
    "CANONICAL_PATH_PREFIXES",
    "CAPTURE_MODES",
    "CAPTURE_STATUSES",
    "EVIDENCE_CLASSES",
    "FINDING_SEVERITIES",
    "FORBIDDEN_BODY_KEYS",
    "FORMAT_VERSION",
    "HASH_URN_RE",
    "INDEX_ARTIFACT_KIND",
    "INTEGRITY_STATUSES",
    "MAX_PACKET_BYTES",
    "PARSER_VERSION",
    "PROJECTION_PACKET_FIELDS",
    "REPAIR_CLASSES",
    "REQUIRED_TOP_LEVEL_FIELDS",
    "SAFE_ID_RE",
    "SHA256_RE",
    "TOOLS",
    "canonical_evidence_ref_hashes",
    "canonical_evidence_refs",
    "collect_session_audit_directory",
    "expected_audit_id",
    "expected_index_id",
    "project_valid_packet",
    "sanitize_collection_summary",
    "sha256_file",
    "validate_collection_projection",
    "validate_session_audit_index",
    "validate_session_audit_packet",
    "verify_canonical_evidence_refs",
    "verify_deterministic_source_projection",
    "verify_packet_source",
]
