interface:
  display_name: "Orchestrate Task Cycle"
  short_description: "Run governed cycles with routing gates"
  default_prompt: "Use $orchestrate-task-cycle in Korean to run a governed task cycle with repo adapters, adapter routing checks, code convention contracts, semantic structure audit, loopback, derivation, validation, commit, and report."
