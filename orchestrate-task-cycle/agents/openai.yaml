interface:
  display_name: "Orchestrate Task Cycle"
  short_description: "Run compiler-first governed task cycles"
  default_prompt: "Use $orchestrate-task-cycle to compile and advance a governed cycle through registered executors, exact bindings, and explicit stop boundaries."
