interface:
  display_name: "Orchestrate Task Cycle"
  short_description: "Continue governed task cycles within one session"
  default_prompt: "Use $orchestrate-task-cycle to continue a governed compiler-first task cycle within a bounded approved session."
