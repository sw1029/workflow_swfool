interface:
  display_name: "Orchestrate Task Cycle"
  short_description: "Run governed cycles with compact packets"
  default_prompt: "Use $orchestrate-task-cycle to advance a governed cycle with script-built packets, bounded semantic judgments, validated receipts, and explicit stop boundaries."
